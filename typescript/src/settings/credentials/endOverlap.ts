/**
 * Credentials deep-link for End overlap.
 * Danger zone is the canonical home. These confirm strings and
 * `DangerZoneService.execute` are the only implementation.
 */
export {
  DANGER_ZONE_CONFIRM,
  OVERLAP_PREVIOUS_ENV,
  dangerZoneCardHref,
  type DangerZoneActionId,
} from "../dangerZone/confirm.js";
