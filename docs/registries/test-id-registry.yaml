# Catalog of TEST-* IDs that action families in
# docs/traceability/action-family-registry.yaml may reference under
# traceability.test_ids. An ID must appear here before an action family
# can cite it, and it must name a real, committed test.
#
# Empty for now: every action family in the registry is lifecycle_status
# planned (no tool is wired into the live allowlist yet — see
# typescript/src/actions/toolExecutionFactory.ts), so there is nothing
# implemented to attach a real test to. Add entries here only alongside the
# real test file/case they name.
schema_version: "1.0"

tests: []
