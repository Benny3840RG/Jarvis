[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "benny-jarvis"
version = "0.1.0"
description = "A practical local command-line assistant scaffold."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Benny" }]
dependencies = []

[project.scripts]
jarvis = "jarvis.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
