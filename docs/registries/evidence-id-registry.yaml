# Catalog of EVD-* IDs that action families in
# docs/traceability/action-family-registry.yaml may reference under
# traceability.evidence_ids. Each entry points to a real committed evidence
# artifact; immutable final workflow run IDs are also recorded in the PR.
schema_version: "1.0"

evidence:
  - id: EVD-AM-003-001
    path: docs/evidence/am-003-commissioning.md
    description: AM-003 implementation, test coverage, runtime CI gate and safety-boundary record
